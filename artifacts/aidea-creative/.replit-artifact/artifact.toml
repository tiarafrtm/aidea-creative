kind = "web"
previewPath = "/"
title = "AideaCreative - Studio Foto"
version = "1.0.0"
id = "artifacts/aidea-creative"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25636

[services.development]
run = "pnpm --filter @workspace/aidea-creative run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aidea-creative", "run", "build" ]
publicDir = "artifacts/aidea-creative/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25636"
BASE_PATH = "/"
